import { Headers } from "../component/Headers";
import { Content } from "../component/Content";

export const Counter = () => {
  return (
    <div>
      <Headers />
      <Content />
    </div>
  );
};
